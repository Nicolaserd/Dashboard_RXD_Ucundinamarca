import { PantallaCarga } from "@/components/layout/PantallaCarga";

/** Carga de nivel raíz: portada de temas, consolidado y la primera entrada a un tema. */
export default function Loading() {
  return <PantallaCarga />;
}
